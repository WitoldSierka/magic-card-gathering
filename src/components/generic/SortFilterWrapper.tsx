import React from "react"
import CardTemplate from "../CardTemplate"

interface MyProps{
  arr: CardTemplate[];
  whatSort: string;
  whatFilter: string;
  //children?: React.ReactNode;
}

const SortFilterWrapper: React.FC<MyProps> = (props) => {


  return (
    <></>
  )
}

export default SortFilterWrapper;